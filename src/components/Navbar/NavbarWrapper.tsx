import styled from "@emotion/styled";
import { SC } from "~root/types/components";

const NavbarWrapper: SC<HTMLElement> = styled.nav`
  display: flex;
  justify-content: center;
  align-items: center;
  height: 50px;
`;

export default NavbarWrapper;
